# Emoji-Named Sticker Images

Put final sticker image files in this folder.

Use the emoji analogue as the file name, for example:

- `smile.png`
- `heart.png`
- `fire.png`

After the files are here, update `src/data/stickers.ts` so each sticker points to
`/stickers/emojis/<emoji-name>.<ext>`.
